import type { StatutEquipe } from '../api/equipe';

export const STATUT_EQUIPE_LABELS: Record<StatutEquipe, string> = {
  inscrite: 'Inscrite',
  enrolee: 'Enrôlée',
  engagee: 'Engagée',
  retiree: 'Retirée',
};
